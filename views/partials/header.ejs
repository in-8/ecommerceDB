
<nav class="navbar navbar-expand-lg pt-3 fixed-top" style="background-color: #19376D;">
    <div class="container-fluid " >
      <a class="navbar-brand fs-1 btn text-white"  href="\">LELO</a>
      <button class="navbar-toggler order-last" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon navbar-dark"></span>
      </button>
      <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
        <ul class="nav navbar-nav me-1 mb-2 mb-lg-0">
          <li class="nav-item dropdown btn btn-light me-sm-2">
            <b class="nav-link dropdown-toggle"  role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    welcome <%= data.name %> 
            </b>
            <ul class="dropdown-menu">
                <% if(!data.session) {%>
                    <li><a class="dropdown-item" href="/login">LOGIN</a></li>
                <% }
                 else {%>
                    <li><a class="dropdown-item" href="/logout">LOGOUT</a></li>
                <% } %>
                <li><a class="dropdown-item" href="login/resetPassword">Reset Password</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="/userOrders">My Orders</a></li>
                <% if(data.role==3 ) {%>        <!--  role 3 is seller in database -->
                  <li><a href="/seller/orders" class=" dropdown-item "> Orders</a></li>
                <% } %>
            </ul>
          </li>
        </ul>
        <form class="d-flex" role="search">
          <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-light" type="submit">Search</button>
        </form>
      </div>
      <% if(data.isAdmin) {%>
        <a href="/admin" class="btn btn-info fs-4 m-2"> Admin</a>
      <%} else if(data.role==3) {%>        <!--  role 3 is seller in database -->
        <a href="/seller" class="btn btn-dark m-2"> Seller Panel</a>
      <% } %>
      <div class="me-3 me-sm-5 ms-3 position-relative">
        <a href="/cart"><img src="../../trolley.png" alt="add to cart image" height="50vh" width="40vh"></a>
        <span id="qtyBadge" class="position-absolute top-0 start-90 translate-middle badge rounded-pill bg-danger">
          <%= quantity %>
        </span>
      </div>
    </div>
  </nav>